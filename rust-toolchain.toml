[toolchain]
channel = "1.95"
components = ["rustfmt", "clippy"]
# Versão homologada da tese (F1): cyclonedds exige >= 1.85; CI e imagem
# usam esta mesma versão. Não voltar para "stable" sem re-homologar.
